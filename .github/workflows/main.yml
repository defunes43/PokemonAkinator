name: Build and Deploy
on: [push]
permissions:
  contents: write
jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }} 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: JSON generation
        run: |


      - name: Install and Build 
        run: |
          npm ci
          npx tsc scripts/xlsx-to-json.ts --esModuleInterop --module commonjs --outDir ./dist_scripts --lib es2020
          node dist_scripts/xlsx-to-json.js
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build # The folder the action should deploy.
