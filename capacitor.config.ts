import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.visconte.pokemonAkinator',
  appName: 'PokemonAkinator',
  webDir: 'build'
};

export default config;
